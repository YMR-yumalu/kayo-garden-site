# Kayo Garden HP｜残タスク

最終更新：2026-09-25

---

## ⏳ クライアント確認待ち（情報が届いたら差し替える）

### ① ロゴが届いたら
| 差し替え先 | 現在 |
|---|---|
| `favicon.svg` | 暫定の「KG」モノグラム（セージ背景） |
| `img/ogp.jpg` | 動画の1コマ＋テキスト。ロゴ入りに作り直したい |
| ヒーロー（任意） | いまはテキストのみ。ロゴを載せる選択肢あり |

### ② 営業時間・定休日が決まったら
差し替えるのは **3か所**（漏れやすいので注意）
1. `index.html` の「アクセス」セクション … `class="tbd"` の `準備中` を置き換え、`tbd` クラスを外す
2. `llms.txt` の「基本情報」… `営業時間：準備中` / `定休日：準備中`
3. `index.html` の JSON-LD … `openingHoursSpecification` を追加する

```json
"openingHoursSpecification": [{
  "@type": "OpeningHoursSpecification",
  "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"],
  "opens": "10:00",
  "closes": "19:00"
}]
```

### ③ Instagramが決まったら
1. `index.html` の「アクセス」… `Instagram / 準備中` を置き換え
2. `index.html` の JSON-LD … `sameAs` の配列にURLを追加
3. `llms.txt` にも追記

### ④ オンラインヨガの詳細が決まったら
`index.html` の `<section class="sec online">` 内の `<!-- TODO -->` 部分を書き換え。
受講方法・使用ツール（Zoom等）・当日の流れ。

### ⑤ 電話番号を出す場合
JSON-LD に `"telephone": "+81-xx-xxxx-xxxx"` を追加。
※ 完全予約制でLINE一本化なら不要。

---

## 🌐 公開したら必ずやること

**`SITE_URL` という文字列が7か所**残っています。公開URLに一括置換してください。

| ファイル | 箇所 |
|---|---|
| `index.html` | 5か所（canonical / og:url / og:image / twitter:image / JSON-LDのurl・image） |
| `robots.txt` | 1か所（Sitemap行） |
| `sitemap.xml` | 1か所（loc） |

置換コマンド（例）：
```bash
cd ~/Desktop/KayoGarden_HP
grep -rl "SITE_URL" . | xargs sed -i '' 's|SITE_URL|https://実際のURL|g'
```

公開後の確認：
- [ ] LINEでURLを送って、OGP画像が出るか
- [ ] スマホで表示崩れがないか
- [ ] 動画が自動再生されるか（モバイル通信時も）
- [ ] Google Search Console に登録して sitemap を送信

---

## 💰 営業まわり

- [ ] 香世さんに提案する（**3万円**の見積もり）
- [ ] 掲載許可をもらう（ポートフォリオに載せるため）
- [ ] 保守の話（更新対応：月3,000〜5,000円）

---

## 📝 決めたこと（変更しない方針）

- **料金はテキストで表示する**（画像にしない）
  - 理由：料金変更のたびに画像を作り直さずに済む／検索に引っかかる／スマホで読みやすい
  - そのため `img/menu_yoga.png` 等の3枚と `botanical_sage.png` は未使用のまま
- **JavaScriptは使わない**（アニメーションは全てCSS）
- 未確定の項目は「準備中（COMING SOON）」で枠だけ用意する

---

## 🎨 実装メモ（引き継ぎ用）

- ブランドカラーは `style.css` の `:root` にまとめてある。色を変えるならそこだけ触ればOK
- スクロールアニメは `animation-timeline: view()`（2026年Baseline）。未対応ブラウザでは普通に表示されるだけで壊れない
- ヒーロー動画は **元データに黒帯が焼き込まれていた**ため、`crop=1280:534:0:88` で切り落とし済み。元は `img/top-movie_元データ.mp4`
- `isolation:isolate` は使わないこと（z-index が壊れる）
